# Railway deployment configuration
# See: https://docs.railway.app

[build]
# NIXPACKS auto-detects Node.js and uses Procfile + package.json

[deploy]
# Procfile takes precedence for start command and release hooks

